<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= heading %></title>
    <!-- ПУТЬ БЫЛ УЖЕ ВЕРНЫЙ -->
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <header>
        <h1><%= heading %></h1>
        <nav>
            <a href="/">Вернуться к списку</a>
        </nav>
    </header>

    <main>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/<%= locals.book.id ? 'update/' + book.id : 'add' %>" method="POST" class="book-form">
            <div class="form-group">
                <label for="title">Название книги:</label>
                <input type="text" id="title" name="title" value="<%= locals.book.title ? book.title : '' %>" required>
            </div>

            <div class="form-group">
                <label for="author">Автор:</label>
                <input type="text" id="author" name="author" value="<%= locals.book.author ? book.author : '' %>" required>
            </div>

            <div class="form-group">
                <label for="review">Отзыв:</label>
                <textarea id="review" name="review" rows="5"><%= locals.book.review ? book.review : '' %></textarea>
            </div>

            <div class="form-group">
                <label for="rating">Рейтинг (от 1 до 5):</label>
                <input type="number" id="rating" name="rating" min="1" max="5" value="<%= locals.book.rating ? book.rating : '' %>">
            </div>

            <div class="form-group">
                <label for="date_read">Дата прочтения:</label>
                <input type="date" id="date_read" name="date_read" value="<%= locals.book.date_read ? book.date_read.toISOString().substring(0, 10) : '' %>" required>
            </div>

            <div class="form-group">
                <label for="isbn">ISBN:</label>
                <input type="text" id="isbn" name="isbn" value="<%= locals.book.isbn ? book.isbn : '' %>">
                <small>Международный стандартный книжный номер (например, 9780321765723)</small>
            </div>

            <div class="form-group">
                <label for="cover_url">URL обложки (если не найдено автоматически):</label>
                <input type="url" id="cover_url" name="cover_url" value="<%= locals.book.cover_url ? book.cover_url : '' %>">
                <small>Например: https://covers.openlibrary.org/b/id/9265910-L.jpg</small>
            </div>
            <button type="submit" class="btn"><%= submit %></button>
        </form>
    </main>

    <footer>
        <p>&copy; 2023 Мои Книжные Заметки</p>
    </footer>
</body>
</html>