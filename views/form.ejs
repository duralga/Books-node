<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">    
    <title><%= heading %></title>
    <link rel="stylesheet" href="/styles/main.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <h1><%= heading %></h1>
        <nav>
            <a href="/">Back to List</a>
        </nav>
    </header>

    <main>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/<%= locals.book.id ? 'update/' + book.id : 'add' %>" method="POST" class="book-form">
            <div class="form-group">
                <label for="title">Book Title:</label>
                <input type="text" id="title" name="title" value="<%= locals.book.title || '' %>" required>
            </div>

            <div class="form-group">
                <label for="author">Author:</label>
                <input type="text" id="author" name="author" value="<%= locals.book.author || '' %>" required>
            </div>

            <div class="form-group">
                <label for="review">Review:</label>
                <textarea id="review" name="review" rows="5" placeholder="Share your thoughts about the book..."><%= locals.book.review || '' %></textarea>
            </div>

            <div class="form-group">
                <label for="rating">Rating (1-5):</label>
                <input type="number" id="rating" name="rating" min="1" max="5" value="<%= locals.book.rating || '' %>">
            </div>

            <div class="form-group">
                <label for="date_read">Date Read:</label>
                <input type="date" id="date_read" name="date_read" 
                       value="<%= locals.book.date_read || '' %>" 
                       max="<%= locals.currentDate || '' %>" required>
            </div>

            <div class="form-group">
                <label for="isbn">ISBN:</label>
                <input type="text" id="isbn" name="isbn" value="<%= locals.book.isbn || '' %>" 
                       placeholder="e.g., 9780321765723">
                <small>International Standard Book Number</small>
            </div>

            <div class="form-group">
                <label for="cover_url">Cover URL (if not found automatically):</label>
                <input type="url" id="cover_url" name="cover_url" value="<%= locals.book.cover_url || '' %>"
                       placeholder="https://covers.openlibrary.org/b/id/9265910-L.jpg">
                <small>We'll try to find the cover automatically if you leave this empty</small>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary"><%= submit %></button>
                <a href="/" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </main>

    <footer>
        <p>&copy; 2023 My Book Notes</p>
    </footer>
</body>
</html>